; Requisito de Moodle 4.5: max_input_vars debe ser al menos 5000
; (el default de PHP es 1000). Sin esto, el instalador falla en el
; chequeo de entorno antes de crear ni una tabla.
max_input_vars = 5000
